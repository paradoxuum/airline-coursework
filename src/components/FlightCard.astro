---
import { FlightTable } from "@/components/flights";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/Card";
---

<Card className="mx-8">
  <CardHeader>
    <CardTitle>Flights</CardTitle>
  </CardHeader>
  <CardContent>
    <FlightTable client:only="react" />
  </CardContent>
</Card>
